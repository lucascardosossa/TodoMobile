<?xml version="1.0" encoding="utf-8"?>
<ContentPage 
    xmlns="http://xamarin.com/schemas/2014/forms" 
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
    x:Class="Todo.Views.MainPage" Title="{Binding Title}">
    
    <StackLayout VerticalOptions="CenterAndExpand" Padding="5">
        <Label HorizontalOptions="CenterAndExpand"  Text="Acessar o sistema" />
        <Entry HorizontalOptions="FillAndExpand" Text="{Binding User}" Placeholder="Usuário"/>
        <Entry HorizontalOptions="FillAndExpand" Text="{Binding Password}" IsPassword="true" Placeholder="Senha"/>
        <Button x:Name="login" Command="{Binding SigninCommand}" BackgroundColor="Blue" TextColor="White" Text="Entrar"/>
    </StackLayout>
    
</ContentPage>